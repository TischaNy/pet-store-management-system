import { render } from '@testing-library/react'
import React from 'react'

function Header(){
    return (
        <nav>
            <h1>Petstore</h1>
            
        </nav>
    );
}

export default Header;
